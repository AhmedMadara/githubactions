name: First_workFlow
run-name: "${{ github.actor }}'s label workflow Run"

on: 
  push:
    branches: ["main"]
  workflow_dispatch:

jobs: 
  hello: 
    runs_on: ubuntu_latest 
    steps:
      - name: Say hello 
        run: echo "Hello World"

  bye: 
    runs_on: ubuntu_latest
    steps: 
      - name: Say Baye
        run: echo "Bye World"
